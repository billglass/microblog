<div class="feedForm">
	<form action="/feed" method="POST">
		<div class="feedPost">
			<textarea name="[post]post" maxlength="150" placeholder="Write your post here"></textarea>
		</div>
		
		<div class="feedSub">
			<input type="submit" value="ZAP IT!">
		</div>
	</form>
</div>
<% if current_user %>
	<div class="posts">
		<% current_user.posts.reverse_each do |tweet| %>
			<!-- create div styled like thought bubble, for reach one it will create one.> -->
			<div class="singleThought">
				<p class="thought">
					<%= tweet.post %>
				</p>
<!-- can't get the tweets to number based on user alone; they're numbering based on all of the tweets -->
				<p class="thoughtId">
					<%= "Zap ##{tweet.id}" %>
				</p>
			</div>
		<% end %> 
	</div>
<% end %>